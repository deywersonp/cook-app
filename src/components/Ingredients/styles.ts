import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  horizontalContainer: {
    height: 58,
    maxHeight: 58,
  },
  horizontalIngredientsContent: {
    gap: 12,
    paddingHorizontal: 32,
  },
  verticalIngredientsContent: {
    flexWrap: "wrap",
    flexDirection: "row",
    gap: 12,
    paddingBottom: 20,
  },
});
